# Бот внутри контейнера — uid 1000; каталог ./data на хосте должен принадлежать 1000:1000 (см. README).
services:
  bot:
    build: .
    image: tg-digest-bot:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Персистентный том ./data на хосте; не полагайся на относительный путь внутри контейнера
      DATABASE_PATH: /data/bot.sqlite3
    volumes:
      - ./data:/data
